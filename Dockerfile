# TODO: Task 3.1 - Specify the base image for our Java application using the slim JDK version of OpenJDK 17. Then, set the working directory to /app.


# TODO: Task 3.2 - Copy the project source code into the working directory. Then, add a command to compile the Java source file located at src/main/java/com/example/SimpleWebServer.java.


# TODO: Task 3.3 - Expose port 8080 to allow traffic to the web server. Then, specify the default command to run the application when the container starts. The main class is com.example.SimpleWebServer and the classpath is src/main/java.

